## Build output
bin/
obj/

## IDE files
.vs/
*.user
*.suo

## Local probe output
probe-artifacts/
*.trx

## NuGet packages and local feeds (never commit built packages)
artifacts/
*.nupkg
*.snupkg
local-packages/

## Test output
TestResults/
*.coverage

## Local per-developer telemetry overrides. The repository-level opt-out in keelmatrix.telemetry.json is
## intentionally committed: it is what keeps local development, samples, and the package-consumer run out of
## production demand data. The repository root .env files stay developer-local because they can carry secrets.
.env
.env.local

## Tooling caches
.dotnet/
