## Build output
bin/
obj/

## IDE files
.vs/
*.user
*.suo

## Local probe output
probe-artifacts/
*.trx

## NuGet packages and local feeds (never commit built packages)
artifacts/
*.nupkg
*.snupkg
local-packages/

## Test output
TestResults/
*.coverage

## Local per-developer telemetry overrides. Repository-local telemetry configuration stays untracked; supported
## development and validation entry points set KEELMATRIX_NO_TELEMETRY=1 explicitly. The repository root .env files
## stay developer-local because they can carry secrets.
keelmatrix.telemetry.json
*.telemetry.json
.env
.env.local

## Tooling caches
.dotnet/
