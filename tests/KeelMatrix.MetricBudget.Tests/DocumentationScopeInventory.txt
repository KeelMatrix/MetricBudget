# Closed inventory for bound-mentioning units.
# Format: surface<TAB>normalized unit. The test owns the per-surface floor.
README.md	Run your normal workload, observe the metric series it actually emits and how many distinct values each tag carried, and fail when an instrument identity exceeds an explicit observed-series or per-instrument-identity distinct-value budget - without a collector, exporter, or observability backend.
src/KeelMatrix.MetricBudget/README.md	Run your normal workload, observe the metric series and tag values it actually emits, and fail when an instrument identity exceeds an explicit observed-series or per-instrument-identity distinct-value budget - with no collector, exporter, or observability backend.
src/KeelMatrix.MetricBudget/README.md	The per-instrument-identity `MaxObservedSeries` name is deliberate because it describes what is measured; there is no default budget value that is universally safe, and the package never invents one for you.
src/KeelMatrix.MetricBudget/README.md	A safety bound was reached, so counts are lower bounds, or the session detected an impossible accounting state.
src/KeelMatrix.MetricBudget/README.md	The narrower helpers are useful when one test owns a single concern, such as asserting only a per-instrument-identity tag budget while another test owns the per-instrument-identity series budget.
src/KeelMatrix.MetricBudget/README.md	Bound
src/KeelMatrix.MetricBudget/README.md	`MaxTrackedSeries` (per instrument identity)
src/KeelMatrix.MetricBudget/README.md	Applies per instrument identity. Further distinct series of an instrument at its bound are counted as untracked observations, the report says series tracking is incomplete, and the outcome becomes `ObservationIncomplete`.
src/KeelMatrix.MetricBudget/README.md	`MaxTrackedValuesPerTag` (per instrument identity)
src/KeelMatrix.MetricBudget/README.md	`MaxTrackedSeries` and `MaxTrackedValuesPerTag` both apply per instrument identity rather than once per session, so the whole session retains up to *(number of matched instrument identities) x `MaxTrackedSeries`* series descriptions plus the per-tag value sets.
src/KeelMatrix.MetricBudget/README.md	Size memory from that product, and note that `ObservationIncomplete` is reported as soon as any single instrument reaches its bound.
src/KeelMatrix.MetricBudget/README.md	**`ObservationIncomplete`** - a safety bound was reached.
src/KeelMatrix.MetricBudget/README.md	The report names the bound, the instrument, and how many observations could not be tracked.
src/KeelMatrix.MetricBudget/README.md	Raise the per-instrument-identity `MaxTrackedSeries` or `MaxTrackedValuesPerTag` when the workload is representative, or narrow the workload if the cardinality is the finding you were looking for.
src/KeelMatrix.MetricBudget/README.md	More failure modes, including a tag budget that never triggers and unexplained series counts, are documented at <https://github.com/KeelMatrix/MetricBudget/blob/main/docs/troubleshooting.md>.
docs/DEV.md	The bound-wording guard is closed by design.
docs/DEV.md	A new or reworded bound statement therefore fails until it is deliberately approved.
docs/observed-vs-production-cardinality.md	Counts are exact while the safety bounds hold; when a bound is reached the report says so and the outcome is `ObservationIncomplete` instead of a pass.
docs/observed-vs-production-cardinality.md	Treat a rising observed-series count as a design signal about tag fan-out, and re-run the workload whenever a tag is added or changed.
docs/phase0-probe-evidence.md	Concurrent delivery is accounted for exactly. Observed-series accounting is serialized behind one writer lock, session summaries are consistent snapshots, and the parallel case asserts a strict contract: each of four unselected sessions that is delivered `4 x 5` distinct series must report exactly 20 observed measurements and 20 tracked series, must never report more tracked series than observations, and must never lose a copied tag set. Sessions that select their own instruments observe only their own measurements.
docs/privacy-and-telemetry.md	safety-bound state and structured problem records.
docs/safety-bounds.md	`MaxTrackedSeries` (per instrument identity)
docs/safety-bounds.md	`MaxTrackedValuesPerTag` (per instrument identity)
docs/safety-bounds.md	The series bound is **per instrument identity**, not one shared pool for the session.
docs/safety-bounds.md	A session that matches several instrument identities can therefore retain up to *(number of matched instrument identities) x `MaxTrackedSeries`* series descriptions, plus the per-tag value sets described below.
docs/safety-bounds.md	Size CI memory from that product rather than from a single per-instrument-identity `MaxTrackedSeries` value, and expect `ObservationIncomplete` as soon as **any** instrument identity's bound is reached - the outcome is not deferred until every instrument is full.
docs/safety-bounds.md	What happens when a bound is reached
docs/safety-bounds.md	The report names the bound, the instrument that reached it, and how many observations could not be tracked. Once any instrument reports an exhausted bound, the whole session outcome is `ObservationIncomplete`.
docs/safety-bounds.md	Counts that depend on the exhausted bound become explicit lower bounds: `MetricBudgetInstrumentResult.SeriesTrackingIncomplete`, `MetricBudgetTagResult.ValueTrackingIncomplete`, and `MetricBudgetSafetyReport.IsComplete` state this machine-readably.
docs/safety-bounds.md	A definite budget breach that is already proven is reported as `Violation`; reaching a bound never hides it.
docs/safety-bounds.md	Retained memory grows with tracked series and tracked distinct values, and stops growing when the bounds are reached. Because the series bound applies per instrument identity, the retained series ceiling grows with the number of matched instrument identities: *(matched instrument identities) x `MaxTrackedSeries`* fixed-size digests, plus the per-tag value sets.
docs/safety-bounds.md	You can lower the per-instrument-identity bounds to fail faster on a workload you know well, for example the per-instrument-identity `MaxTrackedSeries = 10_000`.
docs/safety-bounds.md	Keep the bounds above the cardinality you legitimately expect: a bound that is too low turns a valid verification into `ObservationIncomplete`, which is a louder failure, not a false pass.
docs/troubleshooting.md	Invalid *options* fail earlier and louder: starting a session with no rules, with a rule that declares no limit, or with a non-positive safety bound throws `MetricBudgetConfigurationException` before anything is observed.
docs/troubleshooting.md	A safety bound was reached.
docs/troubleshooting.md	The report names the bound, the instrument, and how many observations could not be tracked, and every affected count is a lower bound.
docs/troubleshooting.md	Raise the per-instrument-identity `MaxTrackedSeries` or `MaxTrackedValuesPerTag` when the workload is representative; narrow the workload when the cardinality is the finding you were looking for.
docs/troubleshooting.md	A tag budget never triggers
docs/troubleshooting.md	A measurement can deliver a `null` tag key. Those measurements are counted and reported, but no tag budget can name them, so fix the caller rather than the budget.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The outcome is not , including when nothing was observed, the configuration was rejected, or a safety bound was reached.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	M:KeelMatrix.MetricBudget.Assertions.MetricBudgetReportAssertions.AssertObservedSeriesAtMost(KeelMatrix.MetricBudget.MetricBudgetReport,System.String,System.String,System.Int32) Asserts that one instrument identity stayed within a specific observed-series limit.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	It never claims to bound the cardinality production can produce.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	A rule must configure or at least one tag budget.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.InstrumentBudget.MaxObservedSeries Maximum number of distinct observed series one instrument identity may produce within the session.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The value must be greater than zero, and means no per-instrument-identity series budget is configured for this rule.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	when a series budget for an instrument identity or at least one tag budget is set.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	T:KeelMatrix.MetricBudget.Internal.TagBudgetLimit One declared tag budget in declaration order.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	T:KeelMatrix.MetricBudget.Internal.InstrumentAccount Observed-series and per-tag accounting for one instrument identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The builder is the single place that decides the outcome ladder, so "nothing was verified" can never be reported as a pass: invalid configuration, when an instrument identity matched more than one rule;violation, when a tracked budget was exceeded;observation incomplete, when a safety bound was reached or accounting was inconsistent;no matching instrument, when a rule selected nothing;no measurements observed, when a selected instrument delivered nothing;pass.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	A lower bound when is .
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	Accounting keys are digests rather than the canonical text so that a session never retains raw tag values in memory, and so that the observed-series set stays a fixed size per series.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	Length-prefixed fields always start with an ASCII digit, so no delivered key can produce the bare marker.A value field is {descriptorLength}:{descriptor}.A descriptor is {CLR type full name}:{invariant text}, so int 1 and string "1" are different identities, and a value is the descriptor null.A value whose invariant text is longer than the configured bound becomes {type}#chars={count}#sha256={hex}, so one pathological value cannot inflate an identity.Entries are sorted with and joined with U+001F, so tag order does not change identity.Duplicate keys are retained, so a tag set is a sorted multiset rather than a set.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	Only valid during the callback.Bound applied to a tag value's invariant text.Per-tag accounting fields in delivered order.The canonical tag-set identity text used to derive the observed-series identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	M:KeelMatrix.MetricBudget.Internal.TagIdentity.CreateSeriesKey(KeelMatrix.MetricBudget.Internal.InstrumentIdentity,System.String) Canonical observed-series identity for one instrument identity and one canonical tag set.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetInstrumentResult.ConfiguredMaxObservedSeries Configured maximum observed series for this instrument identity, or when the rule declares no per-instrument-identity series budget.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetInstrumentResult.UntrackedSeriesObservations Measurements whose series could not be tracked because the series safety bound for this instrument identity was already reached.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetInstrumentResult.SeriesTrackingIncomplete Whether the series safety bound for this instrument identity was reached, which means is a lower bound.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	F:KeelMatrix.MetricBudget.MetricBudgetOptions.DefaultMaxTrackedSeries Default number of distinct observed series retained per instrument identity before it reports that series tracking is incomplete.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	This is a safety bound, not a budget or a safe cardinality for any application.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	F:KeelMatrix.MetricBudget.MetricBudgetOptions.DefaultMaxTrackedValuesPerTag Default number of distinct values retained per tag key and instrument identity before it reports that tag value tracking is incomplete.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	This is a safety bound, not a statement about which tag values are acceptable.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetOptions.MaxTrackedSeries Maximum number of distinct observed series the session retains for one instrument identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The bound applies per instrument identity, exactly like .
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The session does not share one series bound across selected instruments.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	Size memory from that product, and expect as soon as any single instrument's bound is reached.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetOptions.MaxTrackedValuesPerTag Maximum number of distinct values retained per tag key and instrument identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	When the bound is reached, further distinct values are counted as untracked observations for that tag key, the report says tracking is incomplete, and the session never presents that state as a pass.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The bound keeps one pathological value from inflating an identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The report names the bound that was reached; the observed counts are lower bounds.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The text contains meter and instrument identity, configured limits, counts, tag keys, and safety-bound state.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetSafetyReport.MaxTrackedSeries Configured bound on distinct observed series retained for one instrument identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	The bound applies per instrument identity, so the series count a whole session can retain is this value multiplied by the number of matched instrument identities, plus the per-tag value sets.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetSafetyReport.MaxTrackedValuesPerTag Configured bound on distinct values retained per tag key and instrument identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetSafetyReport.MaxTagValueLength Configured bound on a tag value's invariant text before it is represented by a stable digest.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetSafetyReport.SeriesTrackingIncomplete Whether the series safety bound for an instrument identity was reached, which makes every observed series count a lower bound.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetSafetyReport.UntrackedSeriesObservations Measurements whose series could not be tracked because the series bound for an instrument identity was already reached.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetSafetyReport.TagValueTrackingIncomplete Whether the per-tag value safety bound for an instrument identity was reached for at least one tag key, which makes those observed distinct-value counts lower bounds.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetSafetyReport.UntrackedTagValueObservations Measurements whose tag value could not be tracked because a per-tag bound was already reached.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	When a bound is reached, the report says tracking was incomplete and the outcome becomes rather than a pass.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetTagResult.IsConfigured Whether a tag budget was declared for this key.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetTagResult.ConfiguredMaxDistinctValues Configured maximum distinct values for this key, or when no budget applies.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetTagResult.ValueTrackingIncomplete Whether the per-tag value bound for this instrument identity was reached for this key, which means the observed count is a lower bound and no within-budget conclusion can be drawn from it.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.MetricBudgetTagResult.UntrackedValueObservations Measurements whose tag value could not be tracked because the safety bound was already reached.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	F:KeelMatrix.MetricBudget.MetricBudgetViolationKind.SafetyLimitReached A safety bound for an instrument identity was reached, so the observed counts are lower bounds.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	A tag budget is matched against the delivered tag key with ordinal comparison.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	A measurement that delivers a tag key is observed and reported, but a key cannot be named by a tag budget.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	P:KeelMatrix.MetricBudget.TagBudget.MaxDistinctValues Maximum number of distinct values this tag key may produce for the instrument identity.
src/KeelMatrix.MetricBudget/bin/Release/{tfm}/KeelMatrix.MetricBudget.xml	M:KeelMatrix.MetricBudget.TagBudget.ToString Returns the configured tag budget in the form "key" <= N distinct values.

MetricBudgetReport.ToDiagnosticString()	MetricBudget observed-cardinality report: INCOMPLETE (safety bound reached; counts are lower bounds)
MetricBudgetReport.ToDiagnosticString()	series tracking incomplete: 1 measurement(s) could not be tracked because the series safety bound for the instrument identity was reached
MetricBudgetReport.ToDiagnosticString()	- series safety bound for the instrument identity reached while observing meter "tests.metricbudget.documentation-scope" version 1.0.0, counter "requests": 1 distinct observed series were retained and 1 measurement(s) could not be tracked. Observed per-instrument-identity series counts are lower bounds; raise per-instrument-identity MaxTrackedSeries or narrow the workload.
