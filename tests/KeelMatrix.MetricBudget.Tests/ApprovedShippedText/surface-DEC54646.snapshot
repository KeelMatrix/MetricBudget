# Security Policy

## Reporting a Vulnerability

If you discover a security vulnerability in the `KeelMatrix.MetricBudget` package, report it privately by either:

1. emailing **keelmatrix@gmail.com**
2. opening a private GitHub security advisory

Do **not** create a public issue for security vulnerabilities.

Useful report details:

- affected package version
- target framework and runtime
- reproduction steps or proof of concept
- impact, and any suggested mitigation if known

Reports are handled best-effort. Valid issues will be investigated and fixed as appropriate for the current
maintained release line.

## Scope notes

The package observes `System.Diagnostics.Metrics` measurements from code running in the same process. It does not
read metric values, it does not retain tag values, and it opens no network connections except the anonymous
telemetry signal described in [PRIVACY.md](PRIVACY.md). A report or assertion message never contains tag values.

## Supported Versions

Security fixes are prioritized for the latest released package line. Older versions may receive fixes on a
case-by-case basis.

## Guidelines

* Do not post details of the security issue in public repositories or issue trackers.
* Include as much information as possible to help us reproduce and resolve the issue.
* We may request additional information or a proof of concept to verify and fix the vulnerability.
